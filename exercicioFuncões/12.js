
function fatorial(num) {

    for (let fatorar = 0; fatorar < num; fatorar++) {
            var ajuste = fatorar
        }

    const total = ajuste + 1
    console.log(total)
}

fatorial(10.5)